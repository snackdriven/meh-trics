-- Simple migration placeholder - data migration will be done via API endpoint
-- This ensures the migration system doesn't fail on cross-database access

-- Add any database-specific setup or modifications here if needed
SELECT 1;
